# CI/CD pipeline: build → push → deploy to Cloud Run.
# Trigger this from a Cloud Build GitHub trigger on the main branch:
#   gcloud builds triggers create github \
#     --repo-owner=PachXX --repo-name=pashxd-backend \
#     --branch-pattern='^main$' --build-config=cloudbuild.yaml
substitutions:
  _REGION: europe-west1
  _REPO_NAME: pashxd
  _SERVICE_NAME: pashxd-api
  # Service account NAME only -- Cloud Build does not recursively expand
  # ${PROJECT_ID} inside another substitution's default value, so the
  # full email is built inline at the point of use (deploy step args)
  # instead of being pre-assembled here.
  _RUNTIME_SA_NAME: pashxd-api-runtime

steps:
  - id: build
    name: gcr.io/cloud-builders/docker
    args:
      - build
      - -t
      - ${_REGION}-docker.pkg.dev/${PROJECT_ID}/${_REPO_NAME}/${_SERVICE_NAME}:${SHORT_SHA}
      - -t
      - ${_REGION}-docker.pkg.dev/${PROJECT_ID}/${_REPO_NAME}/${_SERVICE_NAME}:latest
      - .

  - id: push
    name: gcr.io/cloud-builders/docker
    args:
      - push
      - --all-tags
      - ${_REGION}-docker.pkg.dev/${PROJECT_ID}/${_REPO_NAME}/${_SERVICE_NAME}

  - id: deploy
    name: gcr.io/google.com/cloudsdktool/cloud-sdk:slim
    entrypoint: gcloud
    args:
      - run
      - deploy
      - ${_SERVICE_NAME}
      - --region=${_REGION}
      - --image=${_REGION}-docker.pkg.dev/${PROJECT_ID}/${_REPO_NAME}/${_SERVICE_NAME}:${SHORT_SHA}
      - --service-account=${_RUNTIME_SA_NAME}@${PROJECT_ID}.iam.gserviceaccount.com

images:
  - ${_REGION}-docker.pkg.dev/${PROJECT_ID}/${_REPO_NAME}/${_SERVICE_NAME}:${SHORT_SHA}

options:
  logging: CLOUD_LOGGING_ONLY
